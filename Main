#include "stdafx.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct lista_de_disciplinas
{

};

int main()
{
	
    return 0;
}

/*FILE *alunos;
FILE *professores;
FILE *disciplinas;
alunos = fopen("alunos.txt", "w+");
professores = fopen("professores.txt", "w+");
disciplinas = fopen("disciplinas.txt", "w+");
fclose(alunos);
fclose(professores);
fclose(disciplinas);*/
